using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rotations : MonoBehaviour
{
    public static int HorizontalAngle = -90;
    public static int VerticalAngle = 0;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
        HorizontalAngle = Mathf.Round(transform.rotation.eulerAngles.z);
        VerticalAngle = Mathf.Round(transform.rotation.eulerAngles.y);*/
    }
}
